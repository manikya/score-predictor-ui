import { Options } from 'ng5-slider';
export class SliderOptions{
    ov5op:Options = {floor:0,ceil:50};
    ov10op:Options = {floor:0,ceil:100};
    ov15op:Options = {floor:0,ceil:150};
    ov20op:Options = {floor:0,ceil:200};
    ov25op:Options = {floor:0,ceil:250};
    ov30op:Options = {floor:0,ceil:300};
}