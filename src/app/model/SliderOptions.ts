import { Options } from 'ng5-slider';
export class SliderOptions{
    ov5op:Options = {floor:0,ceil:250};
    ov10op:Options = {floor:0,ceil:250};
    ov15op:Options = {floor:0,ceil:250};
    ov20op:Options = {floor:0,ceil:250};
    ov25op:Options = {floor:0,ceil:250};
    ov30op:Options = {floor:0,ceil:250};

    wkt5op:Options = {floor:0,ceil:10};
    wkt10op:Options = {floor:0,ceil:10};
    wkt15op:Options = {floor:0,ceil:10};
    wkt20op:Options = {floor:0,ceil:10};
    wkt25op:Options = {floor:0,ceil:10};
    wkt30op:Options = {floor:0,ceil:10};
}